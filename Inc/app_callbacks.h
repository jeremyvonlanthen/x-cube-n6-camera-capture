#ifndef APP_CALLBACKS_H
#define APP_CALLBACKS_H
/* Camera (CMW/DCMIPP) frame & vsync callbacks and the USER1 button callback.
 * These override weak symbols and are linked automatically; no app-level API. */
#endif /* APP_CALLBACKS_H */
